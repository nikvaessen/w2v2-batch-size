# @package _global_

defaults:
  - override /data/set: ls960h

train:
  num_steps: 320_000
  save_init_ckpt: false
  save_last_ckpt: true
  save_progress_ckpt: false
  save_best_ckpt: false

optim:
  algo:
    lr: 5e-5
    weight_decay: 0
